# @package _global_
# Full MCMC preset — equivalent to the old "mcmc" tyro config.
# Usage: python simple_trainer.py +experiment=mcmc
defaults:
  - override /strategy: mcmc

init_opa: 0.5
init_scale: 0.1
opacity_reg: 0.01
scale_reg: 0.01
